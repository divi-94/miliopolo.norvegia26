{
  "name": "norvegia-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.16.0",
  "engines": {
    "node": ">=22.20.0"
  },
  "scripts": {
    "dev": "astro dev",
    "validate": "tsx scripts/validate-content.ts",
    "typecheck": "astro check",
    "test": "vitest run",
    "build": "astro build",
    "verify:links": "tsx scripts/verify-links.ts",
    "test:e2e": "playwright test",
    "check": "pnpm validate && pnpm typecheck && pnpm test && pnpm build && pnpm verify:links"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@astrojs/markdown-remark": "^7.2.2",
    "@playwright/test": "1.62.1",
    "@types/node": "24.0.0",
    "astro": "7.2.0",
    "tsx": "4.23.11",
    "typescript": "^6.0.0",
    "vitest": "4.1.10",
    "yaml": "2.9.0"
  }
}
